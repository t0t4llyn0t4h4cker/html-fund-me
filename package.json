{
	"name": "html-fund-me",
	"version": "1.0.0",
	"main": "index.html",
	"license": "MIT",
	"devDependencies": {
		"prettier": "^2.7.1"
	},
	"scripts": {
		"test": "yarn hardhat test",
		"node": "yarn hardhat node",
		"test:staging": "yarn hardhad test --network goerli",
		"lint": "yarn solhint 'contracts/*.sol'",
		"lint:fix": "yarn solhunt 'contracts/*.sol' --fix",
		"format": "yarn prettier --write .",
		"coverage": "yarn hardhat coverage"
	}
}
